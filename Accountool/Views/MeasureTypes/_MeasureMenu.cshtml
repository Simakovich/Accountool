@model IEnumerable<Accountool.Models.Entities.MeasureType>

@foreach (var item in Model)
{
    <p>
        <a href=@Url.Action("GetAllIndicationsByMeasure", "Indications", new { id = item.Id })>@item.Name</a>
    </p>
}